name: Archived IVME Strict Multi-Resolution Research

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive_notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Archived workflow retained as immutable run-history documentation."
          echo "Use ivme-v34-autonomous.yml for active IVME v3.4 research."
