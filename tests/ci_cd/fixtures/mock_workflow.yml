name: Mock Workflow

on:
  push:
    branches: [ test ]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run test
        run: echo "Running test"
