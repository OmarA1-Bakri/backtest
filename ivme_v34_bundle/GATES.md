# Active gate

The branch is at the full-data diagnostic gate. The workflow must pass compilation, lint, 35 deterministic tests, Pine static checks, data audit, and exact strict-signal terminal classification before full candidate evaluation is enabled.
