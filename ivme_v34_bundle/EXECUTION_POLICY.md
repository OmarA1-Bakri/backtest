# Execution policy

The active workflow is TDD-gated and executes committed source exactly. It does not patch code inside GitHub Actions. The diagnostic run must classify every strict-ready signal before the workflow can be switched to consolidated full historical evaluation.
